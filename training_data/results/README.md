# Results

Individual discovery runs write one JSON file under `results/local/` or
`results/cluster/`.

Aggregated CSV files belong under `results/aggregated/`. Human-readable reports
belong under `results/reports/`.

Generated result files are ignored by Git. Keep only this README and small
documentation artifacts.
